# ytdl

A tiny browser platformer built with plain HTML, CSS, and JavaScript.

## Run locally

```bash
python3 -m http.server 8000
```

Then open <http://localhost:8000>.

## Controls

- Move: `A/D` or `Left/Right`
- Jump: `W`, `Up`, or `Space`
- Restart: `R`
